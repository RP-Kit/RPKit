import org.apache.tools.ant.filters.*

repositories {
    mavenCentral()
    jcenter()
}

buildscript {
    
    repositories {
        mavenCentral()
        jcenter()
    }

    dependencies {
        classpath "com.github.jengelman.gradle.plugins:shadow:2.0.1"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.3.21"
        classpath "org.jetbrains.dokka:dokka-gradle-plugin:0.9.9"
    }
}

allprojects {
    apply plugin: "kotlin"
    apply plugin: "maven"
    apply plugin: "org.jetbrains.dokka"
    group = "com.rpkit"
    version = "1.6.0-SNAPSHOT"
}

subprojects {
    repositories {
        mavenCentral()
        maven { url "https://hub.spigotmc.org/nexus/content/groups/public/" }
        maven { url "https://ci.ender.zone/plugin/repository/everything/" }
        maven { url "https://jitpack.io" }
        maven { url "http://repo.jenkins-ci.org/public/" }
        maven { url "https://maven.sk89q.com/repo/"}
    }

    dependencies {
        testCompile group: "io.kotlintest", name: "kotlintest", version: "1.3.4"
    }

    compileKotlin {
        kotlinOptions {
            jvmTarget = '1.8'
        }
    }
}

File propertiesFile = new File("repo.properties")
Properties properties = new Properties()
properties.load(new FileInputStream(propertiesFile))
def repoUserName = properties.getProperty("repo-username")
def repoPassword = properties.getProperty("repo-password")

// APIs

project(":rpk-core") {

    def mysqlConnectorVersion = "8.0.15"
    def sqliteConnectorVersion = "3.25.2"
    def commonsCodecVersion = "1.12"
    def commonsLangVersion = "2.6"
    def guavaVersion = "27.0.1-jre"
    def gsonVersion = "2.8.5"
    def ehcacheVersion = "3.6.3"
    def kotlinVersion = "1.3.21"
    def slf4jVersion = "1.7.25"
    def pircBotXVersion = "2.1"
    def jettyVersion = "9.4.15.v20190215"
    def velocityVersion = "1.7"
    def hikariCPVersion = "3.3.1"
    def githubAPIVersion = "1.95"
    def jooqVersion = "3.11.9"

    dependencies {
        compile group: "mysql", name: "mysql-connector-java", version: mysqlConnectorVersion
        compile group: "org.xerial", name: "sqlite-jdbc", version: sqliteConnectorVersion
        compile group: "commons-codec", name: "commons-codec", version: commonsCodecVersion
        compile group: "commons-lang", name: "commons-lang", version: commonsLangVersion
        compile group: "com.google.guava", name: "guava", version: guavaVersion
        compile group: "com.google.code.gson", name: "gson", version: gsonVersion
        compile group: "org.ehcache", name: "ehcache", version: ehcacheVersion
        compile group: "org.jetbrains.kotlin", name: "kotlin-stdlib-jdk8", version: kotlinVersion
        compile group: "org.jetbrains.kotlin", name: "kotlin-reflect", version: kotlinVersion
        compile group: "org.slf4j", name: "slf4j-jdk14", version: slf4jVersion
        compile group: "org.pircbotx", name: "pircbotx", version: pircBotXVersion
        compile group: "org.eclipse.jetty", name: "jetty-server", version: jettyVersion
        compile group: "org.eclipse.jetty", name: "jetty-servlet", version: jettyVersion
        compile group: "org.eclipse.jetty", name: "jetty-servlet", version: jettyVersion
        compile group: "org.eclipse.jetty", name: "jetty-webapp", version: jettyVersion
        compile group: "org.eclipse.jetty.websocket", name: "websocket-server", version: jettyVersion
        compile group: "org.apache.velocity", name: "velocity", version: velocityVersion
        compile group: "com.zaxxer", name: "HikariCP", version: hikariCPVersion
        compile group: "org.kohsuke", name: "github-api", version: githubAPIVersion
        compile group: "org.jooq", name: "jooq", version: jooqVersion
    }

    apply plugin: "com.github.johnrengelman.shadow"

    shadowJar {
        classifier = ""
        from sourceSets.main.output
    }

    artifacts {
        archives shadowJar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":rpk-core").version
            pom.artifactId = project(":rpk-core").name
            pom.groupId = project(":rpk-core").group

        }
    }

}

// Bukkit interfaces

project(":bukkit:rpk-core-bukkit") {

    dependencies {
        compile group: "org.spigotmc", name: "spigot-api", version: "1.13.2-R0.1-SNAPSHOT"
        compile group: "net.milkbowl.vault", name: "VaultAPI", version: "1.6"
        compile project(":rpk-core")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    apply plugin: "com.github.johnrengelman.shadow"

    shadowJar {

        dependencies {
            include(project(":rpk-core"))
        }

        classifier = ""
        from sourceSets.main.output
    }

    artifacts {
        archives shadowJar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-core-bukkit").version
            pom.artifactId = project(":bukkit:rpk-core-bukkit").name
            pom.groupId = project(":bukkit:rpk-core-bukkit").group

        }
    }

}

project(":bukkit:rpk-player-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-player-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-player-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-player-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-permissions-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-permissions-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-permissions-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-permissions-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-character-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-character-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-character-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-character-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-chat-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-chat-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-chat-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-chat-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-economy-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-economy-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-economy-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-economy-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-bank-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-bank-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-bank-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-bank-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-shop-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
        compile project(":bukkit:rpk-bank-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-shop-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-shop-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-shop-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-auction-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-auction-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-auction-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-auction-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-stat-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-stat-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-stat-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-stat-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-food-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-food-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-food-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-food-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-payment-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
        compile project(":bukkit:rpk-bank-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-payment-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-payment-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-payment-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-warp-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-warp-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-warp-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-warp-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-kit-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-kit-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-kit-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-kit-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-drink-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-drink-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-drink-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-drink-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-location-history-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-location-history-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-location-history-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-location-history-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-tracking-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-tracking-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-tracking-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-tracking-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-daily-quote-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-daily-quote-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-daily-quote-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-daily-quote-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-lock-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-lock-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-lock-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-lock-lib-bukkit").group

        }
    }

}


project(":bukkit:rpk-feature-flag-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-feature-flag-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-feature-flag-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-feature-flag-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-experience-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }
    
    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {
            
            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-experience-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-experience-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-experience-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-skill-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-skill-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-skill-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-skill-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-class-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-stat-lib-bukkit")
        compile project(":bukkit:rpk-skill-lib-bukkit")
        compile project(":bukkit:rpk-experience-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-class-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-class-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-class-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-block-log-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-block-log-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-block-log-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-block-log-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-unconsciousness-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-unconsciousness-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-unconsciousness-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-unconsciousness-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-moderation-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-moderation-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-moderation-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-moderation-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-selection-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-selection-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-selection-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-selection-lib-bukkit").group

        }
    }

}

project(":bukkit:rpk-store-lib-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-store-lib-bukkit").version
            pom.artifactId = project(":bukkit:rpk-store-lib-bukkit").name
            pom.groupId = project(":bukkit:rpk-store-lib-bukkit").group

        }
    }

}

// Bukkit implementations

project(":bukkit:rpk-players-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-chat-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-players-bukkit").version
            pom.artifactId = project(":bukkit:rpk-players-bukkit").name
            pom.groupId = project(":bukkit:rpk-players-bukkit").group

        }
    }

}

project(":bukkit:rpk-permissions-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-permissions-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {
            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-permissions-bukkit").version
            pom.artifactId = project(":bukkit:rpk-permissions-bukkit").name
            pom.groupId = project(":bukkit:rpk-permissions-bukkit").group

        }
    }

}

project(":bukkit:rpk-characters-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-permissions-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-characters-bukkit").version
            pom.artifactId = project(":bukkit:rpk-characters-bukkit").name
            pom.groupId = project(":bukkit:rpk-characters-bukkit").group

        }
    }

}

project(":bukkit:rpk-chat-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-permissions-lib-bukkit")
        compile project(":bukkit:rpk-drink-lib-bukkit")
        compile project(":bukkit:rpk-chat-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-chat-bukkit").version
            pom.artifactId = project(":bukkit:rpk-chat-bukkit").name
            pom.groupId = project(":bukkit:rpk-chat-bukkit").group

        }
    }

}

project(":bukkit:rpk-economy-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-economy-bukkit").version
            pom.artifactId = project(":bukkit:rpk-economy-bukkit").name
            pom.groupId = project(":bukkit:rpk-economy-bukkit").group

        }
    }

}

project(":bukkit:rpk-banks-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
        compile project(":bukkit:rpk-bank-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-banks-bukkit").version
            pom.artifactId = project(":bukkit:rpk-banks-bukkit").name
            pom.groupId = project(":bukkit:rpk-banks-bukkit").group

        }
    }

}

project(":bukkit:rpk-trade-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
        compile project(":bukkit:rpk-food-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-trade-bukkit").version
            pom.artifactId = project(":bukkit:rpk-trade-bukkit").name
            pom.groupId = project(":bukkit:rpk-trade-bukkit").group

        }
    }

}

project(":bukkit:rpk-shops-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
        compile project(":bukkit:rpk-bank-lib-bukkit")
        compile project(":bukkit:rpk-shop-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-shops-bukkit").version
            pom.artifactId = project(":bukkit:rpk-shops-bukkit").name
            pom.groupId = project(":bukkit:rpk-shops-bukkit").group

        }
    }

}

project(":bukkit:rpk-auctions-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
        compile project(":bukkit:rpk-auction-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-auctions-bukkit").version
            pom.artifactId = project(":bukkit:rpk-auctions-bukkit").name
            pom.groupId = project(":bukkit:rpk-auctions-bukkit").group

        }
    }

}

project(":bukkit:rpk-stats-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-stat-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-stats-bukkit").version
            pom.artifactId = project(":bukkit:rpk-stats-bukkit").name
            pom.groupId = project(":bukkit:rpk-stats-bukkit").group

        }
    }

}

project(":bukkit:rpk-food-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-food-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-food-bukkit").version
            pom.artifactId = project(":bukkit:rpk-food-bukkit").name
            pom.groupId = project(":bukkit:rpk-food-bukkit").group

        }
    }

}

project(":bukkit:rpk-payments-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-economy-lib-bukkit")
        compile project(":bukkit:rpk-bank-lib-bukkit")
        compile project(":bukkit:rpk-payment-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-payments-bukkit").version
            pom.artifactId = project(":bukkit:rpk-payments-bukkit").name
            pom.groupId = project(":bukkit:rpk-payments-bukkit").group

        }
    }

}

project(":bukkit:rpk-essentials-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-warp-lib-bukkit")
        compile project(":bukkit:rpk-kit-lib-bukkit")
        compile project(":bukkit:rpk-drink-lib-bukkit")
        compile project(":bukkit:rpk-location-history-lib-bukkit")
        compile project(":bukkit:rpk-tracking-lib-bukkit")
        compile project(":bukkit:rpk-daily-quote-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-essentials-bukkit").version
            pom.artifactId = project(":bukkit:rpk-essentials-bukkit").name
            pom.groupId = project(":bukkit:rpk-essentials-bukkit").group

        }
    }

}

project(":bukkit:rpk-locks-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-lock-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-locks-bukkit").version
            pom.artifactId = project(":bukkit:rpk-locks-bukkit").name
            pom.groupId = project(":bukkit:rpk-locks-bukkit").group

        }
    }

}

project(":bukkit:rpk-travel-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-warp-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-travel-bukkit").version
            pom.artifactId = project(":bukkit:rpk-travel-bukkit").name
            pom.groupId = project(":bukkit:rpk-travel-bukkit").group

        }
    }

}

project(":bukkit:rpk-feature-flags-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-feature-flag-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-feature-flags-bukkit").version
            pom.artifactId = project(":bukkit:rpk-feature-flags-bukkit").name
            pom.groupId = project(":bukkit:rpk-feature-flags-bukkit").group

        }
    }

}

project(":bukkit:rpk-rolling-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-rolling-bukkit").version
            pom.artifactId = project(":bukkit:rpk-rolling-bukkit").name
            pom.groupId = project(":bukkit:rpk-rolling-bukkit").group

        }
    }

}

project(":bukkit:rpk-experience-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-experience-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-experience-bukkit").version
            pom.artifactId = project(":bukkit:rpk-experience-bukkit").name
            pom.groupId = project(":bukkit:rpk-experience-bukkit").group

        }
    }

}

project(":bukkit:rpk-skills-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-skill-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-skills-bukkit").version
            pom.artifactId = project(":bukkit:rpk-skills-bukkit").name
            pom.groupId = project(":bukkit:rpk-skills-bukkit").group
        }

    }

}

project(":bukkit:rpk-classes-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-stat-lib-bukkit")
        compile project(":bukkit:rpk-skill-lib-bukkit")
        compile project(":bukkit:rpk-experience-lib-bukkit")
        compile project(":bukkit:rpk-class-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-classes-bukkit").version
            pom.artifactId = project(":bukkit:rpk-classes-bukkit").name
            pom.groupId = project(":bukkit:rpk-classes-bukkit").group

        }
    }

}

project(":bukkit:rpk-drinks-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-drink-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-drinks-bukkit").version
            pom.artifactId = project(":bukkit:rpk-drinks-bukkit").name
            pom.groupId = project(":bukkit:rpk-drinks-bukkit").group

        }
    }

}

project(":bukkit:rpk-block-logging-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-permissions-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-block-log-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-block-logging-bukkit").version
            pom.artifactId = project(":bukkit:rpk-block-logging-bukkit").name
            pom.groupId = project(":bukkit:rpk-block-logging-bukkit").group

        }
    }

}

project(":bukkit:rpk-unconsciousness-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-character-lib-bukkit")
        compile project(":bukkit:rpk-unconsciousness-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-unconsciousness-bukkit").version
            pom.artifactId = project(":bukkit:rpk-unconsciousness-bukkit").name
            pom.groupId = project(":bukkit:rpk-unconsciousness-bukkit").group

        }
    }

}

project(":bukkit:rpk-moderation-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-moderation-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {

            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-moderation-bukkit").version
            pom.artifactId = project(":bukkit:rpk-moderation-bukkit").name
            pom.groupId = project(":bukkit:rpk-moderation-bukkit").group

        }
    }

}

project(":bukkit:rpk-selection-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-selection-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {
            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-selection-bukkit").version
            pom.artifactId = project(":bukkit:rpk-selection-bukkit").name
            pom.groupId = project(":bukkit:rpk-selection-bukkit").group

        }
    }

}

project(":bukkit:rpk-selection-worldedit-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-selection-lib-bukkit")
        compile group: "com.sk89q.worldedit", name: "worldedit-bukkit", version: "6.1.5"
        compile group: "com.sk89q.worldedit", name: "worldedit-core", version: "6.1.4-SNAPSHOT"
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {
            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-selection-worldedit-bukkit").version
            pom.artifactId = project(":bukkit:rpk-selection-worldedit-bukkit").name
            pom.groupId = project(":bukkit:rpk-selection-worldedit-bukkit").group

        }
    }

}

project(":bukkit:rpk-stores-bukkit") {

    dependencies {
        compile project(":bukkit:rpk-core-bukkit")
        compile project(":bukkit:rpk-player-lib-bukkit")
        compile project(":bukkit:rpk-store-lib-bukkit")
    }

    processResources {
        filter ReplaceTokens, tokens: [
                "version": version
        ]
    }

    artifacts {
        archives jar
    }

    uploadArchives {
        repositories.mavenDeployer {
            repository(url: "https://repo.seventh-root.com/artifactory/libs-release-local/") {
                authentication(userName: repoUserName, password: repoPassword)
            }

            pom.version = project(":bukkit:rpk-stores-bukkit").version
            pom.artifactId = project(":bukkit:rpk-stores-bukkit").name
            pom.groupId = project(":bukkit:rpk-stores-bukkit").group

        }
    }

}
